[target.x86_64-unknown-linux-musl]
# Install musl-tools (Ubuntu/Debian: sudo apt install musl-tools)
# or the equivalent for your distro to make x86_64-linux-musl-gcc available.
linker = "x86_64-linux-musl-gcc"
rustflags = ["-C", "target-feature=+crt-static"]
